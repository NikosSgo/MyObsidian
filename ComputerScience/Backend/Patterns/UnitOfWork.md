**Unit of Work** — это паттерн, который:
- **Группирует операции** с базой данных в одну логическую транзакцию
- **Управляет соединением** с БД
- **Обеспечивает согласованность** данных (все операции выполняются или откатываются)
- **Предотвращает race conditions** при параллельных операциях