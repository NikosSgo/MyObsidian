Функция называется **сохраняющей 0**, если на наборе из всех нулей она даёт 0.

То есть:
$$
f(0,0,\dots,0)=0
$$
Обозначается классом: $T_0$

Функция называется **сохраняющей 1**, если на наборе из всех единиц она даёт 1.

То есть:
$$
f(1,1,\dots,1)=1
$$
Обозначается классом: $T_1$
