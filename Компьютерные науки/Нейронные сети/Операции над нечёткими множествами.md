Пусть заданы нечёткие множества  
$\tilde{A}$ и $\tilde{B}$ на универсальном множестве $X$  
с функциями принадлежности $\mu_A(x)$ и $\mu_B(x)$.

---

## 1. Объединение нечётких множеств

Степень принадлежности элемента объединению определяется как максимум:
$$  
\mu_{A \cup B}(x) = \max(\mu_A(x), \mu_B(x)).  
$$

**Интерпретация:** элемент принадлежит объединению в той степени, в какой он принадлежит хотя бы одному из множеств.

---
## 2. Пересечение нечётких множеств

Степень принадлежности элемента пересечению определяется как минимум:

$$
\mu_{A \cap B}(x) = \min(\mu_A(x), \mu_B(x)).  
$$

**Интерпретация:** элемент принадлежит пересечению настолько, насколько он принадлежит обоим множествам.

---
## 3. Дополнение нечёткого множества

Функция принадлежности дополнения определяется как:

$$
\mu_{\overline{A}}(x) = 1 - \mu_A(x).  
$$

**Интерпретация:** чем больше принадлежность к ( A ), тем меньше — к его дополнению.

---
## 4. Разность нечётких множеств

Разность определяется как пересечение с дополнением:

  $$
A \setminus B = A \cap \overline{B}.  
$$

Соответственно:  
$$
\mu_{A \setminus B}(x) = \min(\mu_A(x), 1 - \mu_B(x)).  
$$

---

## 5. Альтернативные операции (T-нормы и S-нормы)

Операции минимума и максимума являются частными случаями более общих операторов.

### T-нормы (для пересечения):
- минимум: $\min(a, b)$
- произведение: $a \cdot b$
- ограниченная разность: $\max(0, a + b - 1)$
    
### S-нормы (для объединения):
- максимум: $\max(a, b)$
- вероятностная сумма: $a + b - a b$;
- ограниченная сумма: $\min(1, a + b)$.
    
---
## 6. Основные свойства операций

Операции над нечёткими множествами обладают следующими свойствами:
- коммутативность;
- ассоциативность;
- монотонность;
- идемпотентность (для min/max).    

---

## 7. Законы де Моргана

Для нечётких множеств выполняются аналоги классических законов:

$$
\begin{aligned}
\overline{A \cup B} = \overline{A} \cap \overline{B},  \\
\overline{A \cap B} = \overline{A} \cup \overline{B}.
\end{aligned}
$$

(при использовании стандартных операций min–max).

---

## Краткая таблица операций

| Операция    | Формула                    |
| ----------- | -------------------------- |
| Объединение | ( \max(\mu_A, \mu_B) )     |
| Пересечение | ( \min(\mu_A, \mu_B) )     |
| Дополнение  | ( 1 - \mu_A )              |
| Разность    | ( \min(\mu_A, 1 - \mu_B) ) |

---

> Операции над нечёткими множествами обобщают операции классической теории множеств и определяются через функции принадлежности с использованием t-норм и s-норм.