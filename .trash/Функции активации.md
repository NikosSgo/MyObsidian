**Функция активации** — это функция, которая применяется к линейной комбинации входов нейрона:
$$z=∑w_ix_i+b$$
и формирует его выход:
$$y=f(z)$$

То есть функция активации определяет, **как нейрон реагирует на входной сигнал**.
Список функций активации: